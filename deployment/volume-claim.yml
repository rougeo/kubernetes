apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task-pv-claim
  #namespace: default
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
